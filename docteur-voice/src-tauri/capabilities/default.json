{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the tray-only Docteur Voice app. Using each plugin's 'default' permission bundle rather than hand-picking individual permission identifiers, which is the safer choice when the exact identifier strings can't be verified against a live build.",
  "windows": [],
  "permissions": [
    "core:default",
    "global-shortcut:default",
    "notification:default",
    "opener:default",
    "autostart:default"
  ]
}
